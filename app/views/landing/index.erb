<h2 class="tagline">Find modern Platform-as-a-Service cloud hosting providers:</h2>

<form id="searchform" method="post" action="/filter">

  <label>What are you primarily developing on?</label>

  <input type="hidden" id="selected_platform" name='platform' />
  <ul>
  <% @platforms.each do |p| %>
    <li class="platform_choice" id="platform_choice_<%= p.slug %>" onclick="search_selected_platform('<%= p.slug %>'); return false;">
      <a><%= p.name %></a>
    </li>
  <% end %>
  </ul>

  <p>
    <label>Where are you?</label>
    <%= country_select :country,:name %><br />
    <small>We'll highlight providers with servers/support from your country.</small>
  </p>
  <input type="submit" value="Search" class="submit" /> <span class="complete_link">or <a href="/list">view list</a></span>
</form>
